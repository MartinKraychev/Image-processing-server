def image_processor(img_location, query_params):
    print(img_location)
    print(query_params)
    print(type(query_params[0]))
    print(type(query_params[0].values()))
    # Processes the img
    # Saves the img
    # Returns the img location and img filename
    return 'test', 'test'
